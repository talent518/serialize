# serialize
C语言实现的数据序列化与反序列化功能
```
android for armv5te build command:
	./autogen.sh && ./configure --prefix=/opt/arm --host=arm-linux-eabi --target=arm-linux-eabi --with-glib=/opt/arm && make clean all install

linux build command:
	./autogen.sh && ./configure
```
